
import { Router  } from "express";

const router:Router= Router();


router.post("/signup",)
router.post("/signin",)

router.get("/users",)

router.get("/user",)
router.put("/user",)
router.delete("/user",)



export default router;