{
  "name": "sketchSheet",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev:http": "cd apps/http-backend && bun run dev",
    "dev:ws": "cd apps/ws-backend && bun run dev",
    "dev:next": "cd apps/frontend && bun run dev",
    "prisma":"cd packages/database && bun prisma migrate deploy "
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.6.1",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
