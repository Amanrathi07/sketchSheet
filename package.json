{
  "name": "sketchSheet",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "start": "bun run prisma && bun run dev",
    "dev": "turbo dev",
    "prisma": "cd packages/database && bun prisma migrate deploy "
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.6.1",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "docker-compose": "^1.3.0"
  }
}
